# Convex deployment URL (get this from `npx convex dev` or the Convex dashboard)
VITE_CONVEX_URL=https://your-deployment-123.convex.cloud

# Docker deployment settings (for docker-compose.prod.yml)
GITHUB_REPOSITORY=your-username/recipes
IMAGE_TAG=latest
PORT=3000
